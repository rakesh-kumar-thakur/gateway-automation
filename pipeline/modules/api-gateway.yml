# ============================================================
# API Gateway Module Manifest
# ============================================================

module:
  name: api-gateway
  description: REST API Gateway - endpoints defined in swagger.yaml
  enabled: true

stack:
  name: api-gateway-stack
  templatePath: aws/api-gateway/template.yaml
  capabilities:
    - CAPABILITY_NAMED_IAM

preDeploy: ~

parameters:
  - key: Environment
    source: ssm
    path: /pipeline/{env}/environment
  - key: StageName
    source: ssm
    path: /pipeline/{env}/stage_name
  - key: ApiName
    source: ssm
    path: /pipeline/{env}/api_name

tags:
  - key: ManagedBy
    value: CodePipeline
  - key: Module
    value: api-gateway

# Pipeline auto-triggers when these paths change
changePaths:
  - aws/api-gateway/**
  - pipeline/modules/api-gateway.yml

# Made with Bob
